
<h4>
  Event:  <%= user_device.event_isopt.held_at.strftime("%Y/%m/%d %H:%M") %> <%= user_device.event_isopt.venue_name %>, User: <%= user_device.user.fullname %>
</h4>
<table class="table table-striped">
  <thead>
    <tr>
      <th>
        Timestamp
      </th>
      <th>
        Action
      </th>
    </tr>
  </thead>
  <tbody>
    <% user_device.user_device_logs.each do |log| %>
      <tr>
        <td>
          <%= log.created_at.strftime("%Y/%m/%d %H:%M:%S") %>
        </td>
        <td>
          <%= log.description %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

